comandos 
pwd=saber donde estoy parado
ls=muestra las carpetas
cd=moverse entre carpetas 
cd ..=volver afuera 
mkdir=crear carpeta
git status=muestra los archivos que cambiaron
git add=para añadir los archivos que quiero con push
git commit -m=comentarios prerpara los archivos en el add para subirlos a git 
git push origin main=subir todos los add 
git rm= borra archivos
git mv=mover o renombrar archivos
python es lenguaje tipado dinámico=la variable se adapta segun el tipo de dato que le pasemos
python es un lenguaje orientado a objetos 
en python A|B o A.union(B)=union
en python A & B o A.intersection(B)=interseccion
C=A-B=diferencia
en python A^B=diferencia simetrica
set(A)=convierte la lista en un cojunto
los conjuntos en python no permiten elementos repetidos
set(A) =elimina duplicados
list() convierte el conjunto otra vez en lista  
heapq.heappushpop=agrega un elemento y a la vez extrae un elemento
heapq.heappop=extrae un elemento
heapq.heappush=agrega un elemento
heapq.heappeek=muestra el tope 